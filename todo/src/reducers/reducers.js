export const initialObj = {
    item: 'Learn about reducers',
    completed: false,
    id: 3892987589
}

export const todoReducers = (state, action) => {
    switch(action.type) {
        default:
            return state;
    }
};